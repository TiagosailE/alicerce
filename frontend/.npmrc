save-exact=true
engine-strict=true
fund=false
ignore-scripts=true
# openapi-typescript's peerDependencies still say typescript ^5.x; it runs
# fine against the pinned 6.0.3 (ADR 0011), the peer range just has not
# caught up yet. Revisit when a release declares 6.x support.
legacy-peer-deps=true
